# Аудит исходников и структуры — 24 сентября 2026

## Объём проверки

Проверены состав исходников, маршрутизация E/F/G, зависимости, контентный модуль, формы, ассеты, CSS-слои, сборка Vite, настройки GitHub Pages и опубликованный репозиторий. Базовое состояние прошло `npm run check`: ESLint, четыре контентных теста, TypeScript и Vite. После реорганизации те же проверки повторены; визуальный контроль выполняется на desktop и mobile.

## Выводы и принятые меры

| Приоритет | Вывод | Решение |
| --- | --- | --- |
| Высокий | В GitHub находилась только собранная страница; локальные исходники не были версионированы. | Исходники вынесены в отдельную ветку `source`, действующая публикация сохранена в `main`. Локальные черновики и архивы исключены из Git. |
| Средний | Корневой вариант G, но README называл выбранной версией F. | Документация обновлена: G — текущий вариант; E/F — контрольные маршруты. |
| Средний | `SiteSections.tsx` содержал восемь несвязанных крупных секций, а один CSS-файл смешивал F и G. | Секции разнесены по отдельным файлам; F и G получили отдельные CSS-слои без изменения правил и порядка каскада. |
| Средний | Тип версии экспортировался из шапки и заставлял другие компоненты зависеть от навигации. | Тип вынесен в `src/types/Variant.ts`. |
| Средний | Автоматическая проверка покрывает контентные факты, но почти не покрывает взаимодействия. | Vitest теперь запускает тесты только из `src/`, а CI проверяет исходную ветку. Выполнена браузерная проверка маршрутов и ключевых сценариев. Перед дальнейшими изменениями анкеты и анимаций стоит добавить постоянные E2E-тесты. |
| Средний | `npm audit` показал две умеренные уязвимости в старом Vitest и `@vitest/mocker`. | Инструмент тестирования обновлён до Vitest 4.1.11. `npm ci`, четыре теста и полный `npm audit` после обновления проходят; уязвимостей не обнаружено. |
| Средний | `global.css` остаётся крупным, а мастер анкеты занимает более 600 строк. | Зафиксировано как долг последующего рефакторинга. Дополнительное разбиение отложено: сейчас оно внесло бы риск в уже утверждённый визуал и состояние анкеты. |
| Средний | Публичные изображения и шрифты дают заметный вес страницы; для многих изображений не задана отложенная загрузка. | Отмечено для отдельной оптимизации с замером на реальном телефоне. Без измерений и визуального контроля загрузка/качество изображений не изменялись. |
| Продуктовый | Анкета завершает локальный сценарий и не принимает заявки на сервере. | Поведение явно документировано. Перед реальным запуском нужна API-интеграция и юридическая проверка текста о персональных данных. |

## Что сохранено

- адреса главной страницы, `/e`, `/f`, `/g`, `/showcase/` и `/motion-preview/`;
- порядок секций, контент и текущий вариант G по умолчанию;
- доступность вкладок, FAQ, мобильного меню и диалогов;
- CSS-правила и порядок их действия, все используемые оригинальные ассеты;
- формат сборки GitHub Pages с `404.html` и `.nojekyll`.

## Результаты повторной проверки

- `npm ci` — установка из lockfile проходит;
- `npm run check` — ESLint, 4/4 теста, TypeScript и сборка проходят;
- `npm audit` — 0 известных уязвимостей на момент проверки;
- итоговый обычный CSS-бандл после разделения стилей сохранил тот же хеш `index-cMA1Dxyp.css`;
- локальные маршруты E/F/G при ширине 430 px содержат девять секций, не имеют битых изображений и горизонтального переполнения; у G проверены переключение формата, анкета и мобильное меню, ошибок консоли нет.

## Ограничение проверки

Браузерная проверка использует локальный браузер/эмуляцию ширины экрана; она не заменяет испытание на физическом устройстве и не подтверждает права на коммерческое использование изображений.
